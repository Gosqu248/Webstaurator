apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: webstaurator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      containers:
        - name: review-service
          image: webstaurator/review-service:latest
          ports:
            - containerPort: 8088
            - containerPort: 9098
          envFrom:
            - configMapRef:
                name: review-service-config
            - secretRef:
                name: review-service-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9098
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9098
            initialDelaySeconds: 30
            periodSeconds: 15
