apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: webstaurator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: webstaurator/delivery-service:latest
          ports:
            - containerPort: 8087
            - containerPort: 9097
          envFrom:
            - configMapRef:
                name: delivery-service-config
            - secretRef:
                name: delivery-service-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9097
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9097
            initialDelaySeconds: 30
            periodSeconds: 15
