apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restaurant-service-allow-ingress
  namespace: webstaurator
spec:
  podSelector:
    matchLabels:
      app: restaurant-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
        - podSelector:
            matchLabels:
              app: order-service
        - podSelector:
            matchLabels:
              app: review-service
      ports:
        - port: 8083
    # Prometheus (namespace webstaurator-monitoring) scrapuje /actuator/prometheus na porcie metryk —
    # bez tej reguły default-deny zablokuje scraping, gdy Prometheus faktycznie tam wyląduje (temat 06).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: webstaurator-monitoring
      ports:
        - port: 9093
